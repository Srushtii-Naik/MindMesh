import { PlaceholderCard } from '@/features/dashboard/components/PlaceholderCard';

export function TodaySummaryCard() {
  return (
    <PlaceholderCard
      title="Today's summary"
      message="Once Task Management is live, what's due today will show up here."
    />
  );
}
