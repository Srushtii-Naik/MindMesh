import { PlaceholderCard } from '@/features/dashboard/components/PlaceholderCard';

export function AIInsightsCard() {
  return (
    <PlaceholderCard
      title="AI insights"
      message="Once the AI Companion is live, proactive suggestions will show up here."
    />
  );
}
