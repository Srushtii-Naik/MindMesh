"""
Shared pagination classes.

Consistent query-parameter conventions across list endpoints per
ARCHITECTURE.md Section 6. Empty scaffold for now — DRF's default
PageNumberPagination is configured in settings until domain endpoints exist.
"""
