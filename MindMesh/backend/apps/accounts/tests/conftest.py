"""
Fixtures for accounts app tests.

`api_client`, `user`, `other_user`, and `auth_client` moved to the root
conftest.py (backend/conftest.py) so every domain app's tests can share
them — see that module's docstring.
"""
